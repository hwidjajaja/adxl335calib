int ledPin = 8;
int delayTime - 500;

void setup()
{
    pinMode(ledPin, OUTPUT);
}

void loop()
{
    digitalWrite(ledPIN, HIGH);
    delay(delayTime);
    digitalWrite(ledPIN, LOW);
    delay(delayTime);
}